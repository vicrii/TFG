version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd front
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: front/dist
    files:
      - '**/*'
  cache:
    paths:
      - front/node_modules/**/* 